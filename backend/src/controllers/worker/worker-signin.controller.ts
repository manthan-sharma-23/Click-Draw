import { Request, Response } from "express";

export const worker_signin_controller = (req: Request, res: Response) => {
  try {
     
  } catch (error) {
    return res.status(404).json({ error });
  }
};
